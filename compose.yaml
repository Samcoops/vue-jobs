services:
  server:
    build:
      context: .
    image: jobs-app-image
    environment:
      NODE_ENV: development
    ports:
      - 3000:3000
    develop:
      watch:
        - path: ./package.json
          action: rebuild
        - path: .
          action: sync
          target: /usr/src/app
